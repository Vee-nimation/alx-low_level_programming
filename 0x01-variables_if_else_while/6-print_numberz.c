#include <stdio.h>

/**
* main - Entry point
* Return: Always 0 (Success)
*/
int main(void)
{
	int vincent;
	for (vincent = 48; vincent < 58; vincent++);
		putchar(vincent);
	putchar('\n');
	return (0);
}
